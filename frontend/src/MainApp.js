import React from "react";
import IssueManager from "./components/IssueManager";

// MainApp component renders the IssueManager component
const MainApp = () => {
  return (
    <div>
      <IssueManager />
    </div>
  );
};

export default MainApp;
